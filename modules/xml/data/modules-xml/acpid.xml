<einit prefix="services-virtual-module"> 
 <daemon id="daemon-acpi"
  name="Advanced Configuration and Power Interface daemon"
  provides="acpid"
  requires="mount-critical"
  prepare="if [ ! -e /proc/acpi ]; then
                echo 'feedback|error|ACPI support has not been compiled into the kernel!';
           fi"
  command="/usr/sbin/acpid --foreground ${configuration_services_acpid_options}"
  execute:reload="killall acpid -HUP &amp;&gt;/dev/null"
  restart="yes"
  need-files="/usr/sbin/acpid"
  variables="configuration-services-acpid/.*" />
</einit>
