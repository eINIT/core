<module>
 <daemon id="daemon-portmap"
  name="Portmap Daemon"
  provides="portmap"
  requires="mount-system:network"
  command="/sbin/portmap -d"
  restart="yes"
  need-files="/sbin/portmap" />
</module>
