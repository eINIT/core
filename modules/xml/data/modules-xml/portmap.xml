<module>
 <v-portmap
  name="Portmap Daemon"
  provides="portmap"
  requires="mount-system:network">

  <execute id="daemon" code="/sbin/portmap -d" />

  <need-files s="/sbin/portmap" />
  <restart b="yes" />
 </v-portmap>
</module>
