<module>
 <daemon id="daemon-powersave"
  name="Powersave"
  provides="powersave"
  requires="mount-critical:hal:acpid"
  command="/usr/sbin/powersaved $(if [ -S /var/run/acpid.socket ]] ; then echo '-f /var/run/acpid.socket' ; fi)"
  restart="yes"
  need-files="/usr/sbin/powersaved" />
</module>
