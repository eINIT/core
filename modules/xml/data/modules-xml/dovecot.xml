<module>
 <v-dovecot
  name="IMAP/POP3 server (Dovecot)"
  provides="dovecot"
  requires="network:mount-critical:mysqld">

  <execute id="enable" code="/usr/sbin/dovecot" />
  <execute id="disable" code="/usr/bin/killall dovecot" />
 </v-dovecot>
</module>
