<module>
 <shell id="shell-sensord"
  name="lm_sensors RRDB logger (sensord)"
  provides="sensord"
  requires="logger:sensors"
  enable="/usr/sbin/sensord ${configuration_services_sensord_options}"
  disable="killall sensord"
  need-files="/usr/sbin/sensord"
  variables="configuration-services-sensord/.*" />
</module>
