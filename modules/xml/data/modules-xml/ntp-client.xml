<einit prefix="services-virtual-module">
 <shell id="shell-clock-ntp-client"
  name="NTP-Client (${configuration_services_ntp_client_command})"
  provides="ntp-client"
  requires="network"
  enable="${configuration_services_ntp_client_command} ${configuration_services_ntp_client_options};
          if [[ $? == '0' ]]; then
                echo 'feedback|notice|time synced with ntp server';
          else
                echo 'feedback|warning|failed to sync time with ntp server';
          fi"
  disable="/bin/true"
  variables="configuration-services-ntp-client/.*" />
</einit>
