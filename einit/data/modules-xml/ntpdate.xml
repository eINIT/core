<einit prefix="services-virtual-module">
 <shell id="shell-clock-ntpdate"
  name="NTPDATE"
  provides="ntpdate"
  requires="network"
  enable="ntpdate -s -b -u pool.ntp.org;
          if [[ $? == '0' ]]; then
                echo 'feedback|notice|time synced with ntp server';
          else
                echo 'feedback|warning|failed to sync time with ntp server';
          fi"
  disable="/bin/true" />
</einit>
