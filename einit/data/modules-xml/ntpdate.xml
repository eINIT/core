<einit prefix="services-virtual-module">
 <shell id="shell-clock-ntpdate"
  name="NTPDATE"
  provides="ntpdate"
  requires="network"
  enable="ntpdate -s -b -u pool.ntp.org &amp;&amp; echo -n 'feedback|notice|time synced with ntp server' || echo -n 'feedback|warning|failed to sync time with ntp server'"
  disable="/bin/true" />
</einit>
