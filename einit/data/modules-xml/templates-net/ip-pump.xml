<einit prefix="services-virtual-module">
 <shell-template id="template-shell-ip-pump"
  name="Using 'pump' to manage ip addresses for interface ${interface}"
  requires="ifctl-${interface}"
  provides="ip-${interface}"
  enable="pump -i ${interface}"
  disable="pump -r ${interface}" />
</einit>
