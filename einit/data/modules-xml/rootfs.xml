<einit prefix="services-virtual-module">
 <shell id="shell-rootfs"
  name="Mount ( / )"
  provides="rootfs"
  before=".*"
  enable="mount -o remount,rw /"
  disable="mount -o remount,ro /"
  restart="no" />
</einit>
