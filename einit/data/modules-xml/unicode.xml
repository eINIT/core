<einit prefix="services-virtual-module">
 <shell id="shell-unicode"
  name="Unicode"
  provides="unicode"
  requires="keyboard:setfont"
  enable="for t in ${configuration_system_setfont_ttys}; do
  	  echo -ne '\033%G' &gt; /dev/tty${t}; done; kbd_mode -u;
	  loadkeys -u ${configuration_peripherals_keyboard_layout};"
  disable="/bin/true"
  variables="configuration-system-setfont/ttys:configuration-peripherals-keyboard/layout" />
</einit>
