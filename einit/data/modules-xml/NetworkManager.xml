<einit prefix="services-virtual-module">
 <daemon id="daemon-NetworkManager"
  name="NetworkManager"
  provides="NM:networkmanager"
  requires="dhcdbd:hal"
  prepare="if [ ! -d /var/run/NetworkManager ] ; then mkdir /var/run/NetworkManager; fi ;
           if [ ! -d /var/run/console ] ; then mkdir /var/run/console ; fi ;
           /bin/true"
  command="NetworkManager --no-daemon"
  restart="yes" />
</einit>
