<einit prefix="services-virtual-module">
 <shell id="shell-sysfs"
  name="Mount ( /sys )"
  requires="rootfs"
  before=".*"
  provides="sysfs"
  enable="mount -t sysfs sysfs /sys"
  disable="/bin/true"
  restart="no" />
</einit>
