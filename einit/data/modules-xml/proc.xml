<einit prefix="services-virtual-module">
 <shell id="shell-proc"
  name="proc - process information pseudo-filesystem"
  requires="rootfs"
  provides="proc"
  enable="mount -t proc proc /proc"
  disable="/bin/true"
  restart="no" />
</einit>
