<einit prefix="services-virtual-module">
 <shell id="shell-proc"
  name="Mount ( /proc )"
  requires="rootfs"
  provides="proc"
  before=".*"
  enable="mount -t proc proc /proc"
  disable="/bin/true"
  restart="no" />
</einit>
