<einit prefix="services-virtual-network">
 <ifctl id="wpa_supplicant"
  enable="echo -n &quot;feedback|notice|Using 'wpa_supplicant' to manage interface.&quot;;
   wpa_supplicant -D ${configuration_network_interfaces_${interface}_driver} -i ${interface} -c ${configuration_network_interfaces_${interface}_config} -B -w;
   wpa_cli -B -i ${interface}"
  disable="echo -n &quot;feedback|notice|Disabling 'wpa_supplicant'.&quot;;
   wpa_cli -i ${interface} terminate"
  variables="configuration-network-interfaces-${interface}/.*" />
</einit>
