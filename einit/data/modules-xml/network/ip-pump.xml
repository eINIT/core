<einit prefix="services-virtual-network">
 <ip id="pump"
  enable="echo &quot;feedback|notice|Using 'pump' to manage ip addresses.&quot;; pump -i ${interface}"
  disable="echo &quot;feedback|notice|Disabling 'pump'.&quot;; pump -r ${interface}" />
</einit>
