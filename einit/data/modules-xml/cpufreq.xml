<einit prefix="services-virtual-module">
 <shell id="shell-cpufreq"
  name="Cpufreq Governor"
  provides="cpufreq"
  before="local"
  after=".*"
  requires="mount-critical:acpid"
  enable="for i in ${configuration_services_cpufreq_cpus};
          	do /usr/bin/cpufreq-set --cpu $i --governor ${configuration_services_cpufreq_governor} ;
          done"
  disable="for i in ${configuration_services_cpufreq_cpus} ;
           	do /usr/bin/cpufreq-set --cpu $i --governor performance ;
           done"
  variables="configuration-services-cpufreq/.*" />
</einit>
